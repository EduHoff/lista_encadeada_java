services:
  exerciciojava_unisenaipr_20260422:
    build: .
    volumes:
      - .:/app
    stdin_open: true
    tty: true
    command: sh -c 'javac -d bin $(find src -name "*.java") && java -cp bin application.Program'